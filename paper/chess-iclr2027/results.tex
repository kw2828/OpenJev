% Generated from verified saved reports; no inference.
\newcommand{\UnionDifferenceQuality}{All twelve fits and 18,432 updates complete; 2 of ten frozen checks pass, so the all-check criterion fails (Table~\ref{tab:union-difference}). Edits minus original WLDN agreement is +0.21 points ordinary and +0.49 shifted, against a required one-point mean advantage on each panel. All 86,016 new/copied predictions replay, all minibatch-index receipts are checked, and all ten conditional source-game bootstrap intervals are recomputed. The maximum replay NLL error is zero. The descriptive 95\% intervals for edits minus WLDN are [-0.64, +1.13] and [-0.12, +1.08] points; both include zero. These are adaptive teacher-imitation results on already exposed panels, not independent confirmation or measured game strength.}
\newcommand{\UnionDifferenceCost}{Complete-native timing retains 1,728 decisions; all 288 distinct seed/root/method choices replay against the authenticated panel predictions. The edits/WLDN median paired ratio is 1.510. This descriptive same-process, shared-host measurement includes board parsing, legal menus, backbone evaluation, native child graphs and the head. It does not establish a hardware-general speed advantage or equal training/inference compute.}
\newcommand{\UnionEditControlInput}{A separate input-only audit and independent NumPy replay cover all 36,864 cached roots and 1,087,523 candidates; the earlier native cache audit is authenticated, not rerun. The rotation changes every candidate and relabels 92.25\% of added/removed direction/color instances. It also breaks reverse-direction class consistency in 99.98\% of candidates. Thus this control is a broader corruption test: a performance difference cannot be attributed uniquely to correct edit placement. A reverse-consistent control would be required for that narrower claim.}
\newcommand{\UnionDifferenceEngineering}{The four-arm engineering screen and audit cover 96 root/seed/arm checks and 2,508 candidate scores, including independent Python graph packing. The maximum batched-versus-individual score discrepancy is $1.91\times10^{-6}$ under a frozen $10^{-5}$ tolerance. All twelve artificial updates and their final states replay exactly. These use first-legal-action targets and establish no chess-quality result.}
\newcommand{\UnionConsistentEngineering}{A separate implemented control rotates raw directed/color classes, then derives incoming flags by transposition. It preserves union support, per-candidate/channel class counts and reverse consistency. A bounded screen reconstructs 128 native training roots and independently checks all 3,920 candidates: every candidate changes, with zero reverse inconsistencies. There are 627 numerical score checks with maximum discrepancy $1.43\times10^{-6}$ against a frozen $10^{-5}$ tolerance. The original head with independently packed flags replays all three artificial updates and the final checkpoint exactly. This control has no trained quality result; its artificial labels need not describe a legal chess successor. The original twelve-fit study retains its declared corrupted-label control.}
\newcommand{\GraphQualityResult}{The completed twelve-fit study retains 18,432 updates and fails its original criterion: 4 of eight checks pass (Table~\ref{tab:graph-contrast}). Contrast minus child-only agreement is +0.16 points ordinary and -0.20 shifted. It improves over the base and trained shuffled-child control but does not establish the required advantage over root-only or child-only scoring. All 73,728 saved predictions replay with zero NLL discrepancy; all ten descriptive bootstrap intervals are recomputed. Root-only checkpoints exactly reproduce original transport for all three seeds.}
\newcommand{\GraphBaselineResult}{The combined fourteen-check criterion also fails: 4 checks pass. WLDN exceeds scalar contrast by 3.60 and 3.48 agreement points on the two panels. The audit authenticates all four model replay audits, joins all 73,728 distinct method/seed/position records, retains all earlier failed checks, and recomputes six source-game intervals. These descriptive development comparisons favor an established nodewise-difference adaptation over the proposed scalar contrast. They provide no independent confirmation, engine-strength result or new algorithmic contribution.}
\newcommand{\NormalizationInputResult}{A separate input-only screen over 128 old training roots finds nonzero inverse-channel mixed differences from separate normalization on 1,157 of 3,920 legal candidates; a fixed untrained linear projection detects 1,146. Independent native-graph and NumPy reconstruction replays every value exactly. Thus an interaction response can arise without learned nonlinear scoring. A common union denominator with diagonal slack cancels the single-step mixed difference to numerical precision, but changes the information path and computational cost and is not a demonstrated policy improvement.}
\newcommand{\WLDNInterventionResult}{All 73,728 intervention/native predictions and 12,288 copied base references replay through the original head with temporary hooks; the native predictions reproduce the original study. Zeroing encoded node differences reduces mean agreement by 5.45 / 5.18 points on ordinary/shifted panels. Shuffling those differences within each legal menu reduces it by 10.63 / 9.15 points. Every native-minus-intervention agreement interval is positive under the declared source-game bootstrap; these ten descriptive intervals do not correct for adaptive development or multiple comparisons.}
\newcommand{\WLDNQualityResult}{The WLDN study completes all three final fits and 4,608 updates; all 24,576 new/copied predictions replay with zero NLL discrepancy. Mean teacher agreement is 37.01\% ordinary and 31.43\% shifted, versus 31.75\% and 26.81\% for the frozen base. Mean WLDN NLL is 2.161 and 2.355. These are descriptive development results for an established-method adaptation; they do not establish a new architecture, engine strength or equal inference compute.}
\newcommand{\PathBaselineResult}{A separately frozen NBFNet-style adaptation exceeds transport by 1.51 and 0.91 agreement points on ordinary and shifted panels (Table~\ref{tab:path-baseline}). Transport passes 0 of its two additional advantage checks; all 36,864 baseline/reference predictions replay.}
\newcommand{\PathBaselineLatency}{Paired complete-decision medians are 0.586, 1.106 and 5.243 ms for the backbone, transport and path head, respectively. The path head takes 4.74 times transport's median in this rotating shared-host measurement; the quality comparison is not a matched-FLOP or matched-latency result.}
\newcommand{\AttackEditInputResult}{An input-only audit and independent replay cover all 1,087,523 cached legal candidates. Every candidate changes the graph and vacates a nonempty own-attack source row; no candidates within a root have identical child graphs. Stationary pieces change attack lines for 62.04\%, 60.62\% and 59.91\% of training, ordinary and shifted candidates, respectively. Thus the exact-zero-change invariant is synthetic on these panels, and a child-assignment shuffle can disrupt simple move/source consistency as well as tactical relations. No fitted-model reliance on either signal is established.}
\newcommand{\DifferenceBaselineEngineering}{Each baseline passes a separate native-input screen over 24 root/seed pairs and 627 candidates. Maximum separate-candidate versus batched score errors are $1.67\times10^{-6}$ for WLDN and $2.62\times10^{-6}$ for the condensed-graph adapter, below their predeclared $10^{-5}$ tolerance. Each preserves the initial backbone exactly and replays three artificial-target updates to an identical checkpoint. These tolerances do not change earlier failed screens. No teacher-agreement, engine or gameplay result is established by these preflights.}
\newcommand{\GraphContrastSharedResults}{A separately frozen implementation shares root propagation and retains compact legal-child operators. Its 96 root/seed and 2,628 candidate checks pass with maximum discrepancy $4.77\times10^{-7}$. Exact zero-change behavior and twelve additional artificial-update checkpoint replays also pass. A lossless bit cache covers 36,864 existing roots and 1,087,523 legal children; every stored graph is independently reconstructed. These engineering checks establish neither trained quality nor an inference-speed advantage.}
\newcommand{\GraphContrastResults}{The numerical screen passes over 96 root/seed checks and 2,628 candidate checks. Maximum root, child and score-difference discrepancies are each at most $4.77\times10^{-7}$ against a fixed $2\times10^{-6}$ tolerance. Identical operators produce exactly zero correction and parameter gradient. Twelve artificial-target optimizer updates check gradient flow and cost; their complete checkpoint replay is exact. Those twelve audit updates are additional computation. No development teacher agreement, engine score or game result is measured for this head.}
\newcommand{\JointResults}{The joint-training criterion fails: 4 of twelve checks pass. Joint transport minus jointly trained direct agreement is +1.51 and +0.02 percentage points on ordinary and shifted panels. Relative to the preceding frozen-representation transport-minus-base margin, this changes the margin by +0.24 and -1.17 points. The audit checks all 27,648 training records and replays all 98,304 evaluation predictions, including graph corruption. Encoder and policy weights change while unused auxiliary/value/projection parameters remain fixed. This matched-update optimization comparison does not establish a new transport mechanism, fresh confirmation or gameplay improvement.}
\newcommand{\BaselineEngineResults}{The new assessment uses 791 fresh calls and 15,820,000 requested nodes. Full transport has lower mean bounded loss than each simpler head on both panels. Raw centipawn loss favors typed overlap on both panels and untyped overlap on the ordinary panel. Thus the mean bounded-loss ranking survives these simpler controls, but there is no uniform ranking across endpoints. The audit checks all 3,840 policy-position records, and fresh backbone/transport means exactly match the preceding engine assessment. No new neural inference or gameplay is performed; this post-hoc extension cannot replace either failed primary criterion.}
\newcommand{\CounterfactualResults}{The untrained screen covers 7,623 candidate/step comparisons over 32 old training roots and three seeds. Maximum distribution error is $3.58\times10^{-7}$, but maximum readout error is $3.81\times10^{-6}$, above the fixed absolute tolerance of $2\times10^{-6}$. Median sparse/dense operator-storage ratio is 0.0675, with lower storage on every root; this is not peak memory. Median paired complete-pipeline latency ratio is 1.410, failing the required ratio of at most 0.90. The original numerical and combined engineering criteria fail. All output/storage records replay; timings are authenticated rather than repeated.}
\newcommand{\RelationBaselineResults}{The additional transport-advantage rule fails: 0 of six comparisons pass. Transport minus generic-head agreement is +0.91 and +0.68 percentage points on the two panels. The audit replays all 36,864 new baseline outputs and 24,576 copied reference outputs. Every paired-seed agreement difference favors transport, but the fixed minimum effect is not reached. This comparison does not change the earlier failed criterion and is not fresh confirmation.}
\newcommand{\LocalSavings}{60.6--72.0}
\newcommand{\BioAbstract}{The biological topology fails its frozen continuation criterion.}
\newcommand{\LocalAbstract}{The untrained screen agrees with full recomputation within $2\times10^{-6}$ while reducing logical recurrent-node updates by 60.6--72.0\%. Its median paired complete-latency ratio is 1.068 relative to full recomputation, so the combined engineering criterion fails.}
\newcommand{\BatchAbstract}{A separately frozen batched baseline is numerically equivalent and makes the original incremental path 2.91 times slower in a paired complete-loop comparison.}
\newcommand{\DeltaDevReduction}{6.02}
\newcommand{\DeltaShiftWorsening}{7.42}
\newcommand{\DeltaDirectPoints}{50.00}
\newcommand{\DeltaActionPoints}{50.52}
\newcommand{\TransferMin}{26.03}
\newcommand{\TransferMax}{26.79}
\newcommand{\BioResults}{The original criterion fails: 5 of 30 constituent checks pass. Biological mean loss reductions against individual rewires range from -12.09\% to +0.29\% across the two panels. These are descriptive paired comparisons, not a population-level significance test.}
\newcommand{\LocalResults}{The median paired incremental/full latency ratio is 1.068 for the factorized network and 1.757 for the broadcast control. The combined engineering criterion fails. No training or game-quality evaluation is performed. Logical savings and numerical equality do not establish a useful learned policy.}
\newcommand{\BatchResults}{All 36,594 batched/single numerical comparisons pass the same tolerance; the largest hidden discrepancy is $3.50\times10^{-7}$ and the largest score discrepancy is $1.19\times10^{-7}$. Median paired batched/full latency ratios are 0.361 (factorized) and 0.355 (broadcast). The original incremental implementation takes 2.906 and 4.812 times the batched baseline, respectively. Its separately fixed requirement of at least 10\% lower latency fails. These shared-host development measurements establish no hardware-general speed ratio.}
\newcommand{\TransportAbstract}{A fifteen-fit comparison of two-endpoint relation transport against routing, overlap and topology controls fails its development teacher-agreement criterion.}
\newcommand{\TransportResults}{The fixed continuation rule fails: 3 of 10 panel/comparator checks pass. Transport's mean agreement changes relative to the unchanged backbone are +1.27 and +1.19 percentage points on ordinary and shifted panels. Relative to the overlap ablation, they are +0.49 and -0.10 points. Local median complete-decision latencies are 0.435 ms for the backbone and 0.842 ms for full transport. The saved-record audit replays all 86,016 evaluation predictions, including test-time topology corruptions, and independently rebuilds the metric arithmetic and original criterion. No training replay, game evaluation or untouched confirmation is performed; a separate exploratory engine assessment follows.}
\newcommand{\TransportRegretResults}{The separate assessment uses 817 fresh engine calls and 16,340,000 requested nodes, with no new neural inference. Full transport reduces mean bounded loss from the backbone by 33.84\% on ordinary positions and 10.84\% on shifted positions. Raw centipawn loss, however, increases by 5.40\% on the shifted panel. The rewired control has lower shifted bounded loss than full transport. The bounded transform saturates at extreme scores, while raw centipawn loss depends on the arbitrary mate-score encoding; these conflicting endpoints prevent a uniform quality-improvement claim. This post-hoc characterization does not alter the failed primary criterion and is not independent confirmation.}
