# Decision: close this qualification recipe

The frozen rule fails: full public memory succeeds in 195 of 256 layout/order pairs, below the required 205. All other 16 checks pass. Full memory exceeds last-32 history by 119 paired successes (46.484375 percentage points), with no comparator-only successes. Both statements are retained.

This is a substantial information-retention effect in explicit rule-based controllers. It is not a learned architecture result. Do not run the conditional neural pilot on this recipe, tune its public planner, change the 80% criterion, replace layouts, shorten windows, or increase the 128-action limit to rescue it.

Keep all 1,280 episodes and exact replay evidence. The broader objective remains active. A future independently motivated environment or mechanism requires a separate prospective experiment; this result is neither erased nor relabeled as a pass.
