# Sparse-query opportunity evidence

**Completed; primary continuation FAIL, 14/16.** All 24 VALID and 360 EVAL paths are retained. [Results](../../research/otto-sparse-query-results.md) include all five arms, three settings, complete costs and both failed primary conditions.

The repository keeps compact plans, test logs, receipts, both audit attempts, supervisor records, full summary reductions and all figure data. The [release archive](https://github.com/kw2828/OpenJev/releases/tag/otto-sparse-query-v1) adds every compressed work, gate, transition, forward and calibration journal, full episode witnesses, all engineering attempts, every figure attempt and all 114 sources bound by the scientific plan. Its manifest records every member's SHA256 and size. Extract at the repository root to restore paths.

The first saved audit failed on a dictionary-versus-scalar cost check. The separately frozen correction passed without changing the experiment, data or criteria; both attempts remain visible. The first figure's overlapping labels are preserved alongside the final shared-legend version. The earlier lint failure and the runner test receipt's explicitly retrospective transcription provenance are also preserved.

This is not a standalone runtime bundle. Eleven inherited model, kernel and prior-evidence inputs are identified by hash in the plan and release manifest but remain in earlier releases. The runtime and ancestral input dependencies are not repackaged. Original absolute-path authenticators remain unchanged; extracting the archive does not qualify a scientific rerun. No new model was trained and no scientific retry occurred.
