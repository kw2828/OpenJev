# Paired value-of-query pilot evidence

Incomplete: the original 900-second allocation ended after all 72 TRAIN paths
and 210 of 256 available label panels. The scientific signal rule was not
evaluated, and no training or evaluation was performed. No retry or budget
extension occurred.

[Attempt report](../../research/otto-query-advantage-results.md) describes the
counts, limits and proposed engineering follow-up.

The repository retains compact plans, engineering attempts and receipts, the
original supervisor terminal, and the worker failure records. The
[release archive](https://github.com/kw2828/OpenJev/releases/tag/otto-query-advantage-v1)
retains every partial payload, including the interrupted panel's event journal,
all saved anchors and all completed panel records. Its manifest lists every
included file's SHA256 and size. Extract at the repository root to restore the
repository-relative layout.

The archive includes this attempt's sources and the 125 source files bound by
its plan. Prior model weights, runtime and inherited input evidence remain
identified by the plan and previous releases. This is not a standalone runtime
bundle. The original authenticators retain absolute paths; relocation alone
does not qualify a rerun.

The frozen success-only scientific auditor and renderer were not run against
these incomplete records. Any metadata-only failure review concerns closure,
integrity, counts and timing, not scientific outcome validity.
