The query-written memory candidate failed its frozen DEV continuation rule:
**6/13 conditions passed. TEST was not admitted or evaluated.**

This release preserves all 18 trained checkpoints, all 24 TRAIN and 24 DEV
prediction files, the complete training payloads, independent saved-output audit,
original process closures, frozen source lineage and a report showing every
method and seed. No alternative method is promoted after observing the result.

Download `otto-query-memory-dev-v1.tar.gz` with its `MANIFEST.json`,
`DEPENDENCIES.json` and `SHA256SUMS`. The required [collection companion](https://github.com/kw2828/OpenJev/releases/tag/otto-query-memory-collection-v1)
contains the original 108 paths and is referenced by exact archive hash.

This is a fixed-path teacher-score prediction experiment, not a claim of
improved autonomous gameplay, calibrated probabilities, compute savings or
architecture novelty. Native inputs and recorded runtime installations remain
external, so the release is not a self-contained executable reproduction.
