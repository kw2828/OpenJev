# Exact-cache diagnostic evidence

The saved replay closed successfully with exact agreement on all 22,296 returned
requests. The fixed continuation screen **failed 3/4**: hit-associated historical
TensorFlow time was 10.64817%, below 40%. No native cache qualification or new
learning experiment was admitted.

- `engineering-01/`: 22 fabricated cache/proxy tests and static checks, passed.
- `proxy-plan-01.json`, `proxy-01/`, `proxy-supervisor-01.*`: one completed
  metadata-only precursor, zero full-precision state hits.
- `replay-engineering-01/`: source-pin preflight mismatch, before any test or
  empirical command was launched. Retained as not started.
- `replay-engineering-02/`: ten fabricated replay tests and static checks,
  passed once after final source review.
- `replay-plan-01.json`, `replay-01/`, `replay-supervisor-01.*`: one completed
  numerical saved-input replay. `requests.jsonl` retains every returned request;
  `summary.json` contains every episode, all twelve cells and all four conditions.
- `figure-01/`: complete aggregate visualization and its input/source hashes.

The original stopped collection under `output/otto-score-forecast-v1/` is
unchanged. Its 45 forecast conditions remain not evaluated. Both diagnostics
read its saved evidence; neither resumes that allocation or fits its TRAIN data.
The numerical replay reconstructs public beliefs and model inputs using NumPy,
but makes no new learned-model, TensorFlow, native-environment or optimizer calls.

The release archive carries these records, code, public kernels, original
journals and the inherited source/evidence material from the preceding report.
See its manifest and `RESTORE.md` for exact members and external dependencies.
Historical plans record this machine's original absolute paths and are preserved
for audit, not rewritten as portable rerun commands.

[Results](../../research/otto-exact-cache-results.md) ·
[Protocol](../../research/otto-exact-cache-protocol.md) ·
[Release](https://github.com/kw2828/OpenJev/releases/tag/otto-exact-cache-v1).
