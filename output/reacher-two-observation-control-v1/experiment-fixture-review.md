# Independent static review of preparation and whole-tree fixture

Reviewed on 2026-09-18. No material integration blocker was found. This review read source and tests only; it did not prepare a plan, derive a scored stream, rerun tests, load a production checkpoint, train a model, or call a native environment.

| Reviewed file | SHA256 |
| --- | --- |
| `src/openjev/research/reacher_two_observation_experiment.py` | `b6e48a21265a6ca996e3544418b832630167ad65833e26b4a376bcfd522636f2` |
| `tests/test_reacher_two_observation_experiment.py` | `6ae203e19af6f507f6aab930fc923fd41beef735da3bf00edcfb30c5f9d9216a` |
| `tests/reacher_two_observation_fixture.py` | `a8c48f071f5914d3018de187ebd81ca070ff844ff7ee64c09bb36929695ffdaf` |

The preparation author reports 43 synthetic byte-tree tests passing in 6.07 seconds and Ruff clean. Those results are attributed, not independently rerun here. The final delta adds the enclosing auditor test's delegated historical Torch410 call to both required attribution and the fixture ledger; the source hashes above include that correction.

The envelope authenticates both parent roles, all 44 reused training/initialization/order/fit files, the completed prerequisite and its terminal binding, current runtime and the complete 116-source closure. The original 90 sources must retain their inherited hashes. Cache lineage must agree across the two parents. Original training fields, including unused MLP-width metadata, remain matched. The narrow engineering-only width relaxation permits the actual tiny parent while scientific width107 stays fixed.

The RNG inventory is reconstructed from the hash-bound historical registry lists and descriptors, their inline exclusions, all five full-size geometry-memory namespaces and the source-bound literal-call ledger. Partial coverage is expanded only for an old engineering parent's own namespace; it is not an allocation of this experiment's scientific namespace. The new scored manifest is constructed after the preparation admission checks. Engineering uses its explicit namespace and cannot consume a scientific freeze. The stream handle is bound to the exact settings. This supports the declared historical inventory and recorded generator states; it does not establish an independent process-level observation of every prior draw.

Preparation returns an unrun, unfrozen envelope. Validation checks original serialized plan bytes against the external SHA, reconstructs the exact envelope, and requires the separate externally authenticated freeze for scientific execution. The scientific path also requires retained, source/runtime-bound rehearsal and capacity evidence with all critical measured components and explicit caps. Receipt authentication is not neural numerical verification or an empirical result. The authoring helper does not fabricate a completed rehearsal or timing cap.

The runner and preparation layer agree on `random_stream_contract`, top-level execution/audit caps, enriched lineage descriptors and the validated-context fields. The runner supplies exact original plan bytes, so its use of absolute filesystem paths does not bypass or conflict with the preparer's path checks. Exact membership is 8,119 payload files plus root `completed.json`, including 24 restoration snapshots, three new fit directories, 50 saved input pairs and all 42 control rows. The new history rows keep their extra controller receipts and explicit completion-wall schema.

The fixture uses the explicitly pinned completed engineering cache and geometry-memory parents. It preserves the original tiny dimensions/recipe, all three new fits, all six inherited models, all nine final classes, all 42 rows, the native 50-action horizon and all five references. Its helper and separate earlier training-integration script are copied and hash-bound as engineering sources. Preparation snapshots current source bytes and revalidates the prepared plan before returning. Run and saved-output audit are separate entry points; no numerical kernel or registry is monkeypatched by the fixture.

This clears the reviewed code for a retained whole-tree engineering rehearsal. It does not assert that such a rehearsal, capacity measurement or new scientific study has completed. The independent enclosing auditor and its actual full-tree results remain separate evidence.
