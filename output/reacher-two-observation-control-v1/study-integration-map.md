# Two-observation study integration map

Read-only source review. No protocol preparation, seed allocation, tests, model/native calls or artifact scoring was performed. The new episode/results components were still being implemented; their final APIs and hashes need separate review. This map does not authorize a launch.

## 1. Reuse kernels, replace study orchestration

| Existing interface | Safe integration boundary / concrete trap |
| --- | --- |
| `reacher_geometry_memory_control.learned_control(plan, model, panel, inputs_by_step, out, deadline, progress, *, cases)` | Directly usable for the inherited `residual_gru` and `cached_gru` classes. It accepts explicit `ControlCase` objects and saved inputs; no new arm may be inserted into its frozen `KINDS` or cache identity registry. |
| `reacher_two_observation_episode.learned_control(...)` | New history-arm native orchestration, with the same positional shape and keyword `cases`. Its additional row receipts/timing schema must be recognized explicitly, not forced through the legacy row schema. Source was in progress at this inspection. |
| `reacher_geometry_physics.PhysicsGeometryCEM.plan(qpos, qvel, public_target, inputs, *, step, deadline)` and `score_bank(...)` | Reuse unchanged nominal dynamics, geometry and four-bank CEM. All candidate and selected-advance arrays remain auditable. |
| `reacher_geometry_memory_study.reference_control(...)` | **Adapt into a new wrapper.** Particle and uniform paths call that module's frozen `protocol.seed`, whose namespace validator rejects this study. Do not rebind its `protocol` global. Preserve public-only observer inputs, issued-command checks, setup/native/storage times and ragged failure preservation. |
| `reacher_search_protocol.save_inputs/load_inputs/save_trace/save_npz`, native record serializers, `search_study.copy_verified` | Reusable explicit-input serialization/copy primitives. The enclosing new manifest and deadlines still own scope and authentication. |
| `audit_reacher_geometry_memory_study.audit_physics_bank/audit_physics_trace`, `replay_nominal_bank`, `audit_kinematic_observer` | Reusable numerical/native checks where the saved physics schema is unchanged. Propagate the new audit deadline through the existing scoped ContextVars and reset tokens afterward. Candidate, selected and observer work remain separate. |
| `audit_reacher_geometry_memory_study.audit_reference_control`, `audit_particle_observer`, `audit_cohort` | Adapt the wrappers: they obtain named seeds/schedules from the old protocol. New code must validate the new registry; no frozen registry substitution. |
| `search_audit.audit_trace` and `audit_innovations` | Reuse unchanged saved CEM arithmetic and input reconstruction with an explicitly authenticated new contract and `planners=['cem256']`. Check all 256 scores, global earliest tie, selected action and shortened horizon. |

Do not call the old top-level `prepare/run_validated/audit_saved`, `validate_members`, `audit_restoration`, `qualification` or `audit_costs` as if they were generic. They hardcode zero new fits, twelve restored models, 51 rows, old source/lineage schemas and old model registries. `audit_learned_control` remains suitable for legacy inherited rows only; its `audit_scoring_trace` hardcodes the old controller version/identity and its state checker rejects the new history buffers.

## 2. Authenticate original pairing before any new fit

The new parent chain has two distinct roles: the completed cache study supplies training/weights, while completed geometry-memory evidence supplies the positive continuation prerequisite, newest consumed streams and cumulative history. Pin the cache plan `7868daa12242df37f020946f9d3b279811a0e97547eef4d8b179da6e9394cefa`, prerequisite plan `23c93e4adfbb45cf224383ffa31d7323df2405807eb3f1c4c75bb27378bcd9ee` and terminal receipt `8c65730d6547b118eaeaca86ef61857ef0e4f52145f3eea214316f23f32dbae2`; follow their hashes to completed execution, audit, summary and source maps. The old cache failure remains historical, not a positive prerequisite.

Copy the protocol's exact 44 members: `train.npz/json`, all three `initializations/pairN.pt/json` and `orders/pairN.pt/json`, and all five files for each of six inherited `fits/{residual_gru,cached_gru}-pairN` directories. Add authenticated lineage sidecars separately. **Do not use `reacher_cache_study.prepare_training`: it generates new tensors and orders.**

For each pair, validate the full sealed original initialization/order payload and their historical roles/settings/seeds, then extract `initialization['states']['gru']`. Its canonical tensor hash must match the original family hash and each corresponding inherited `initial-weights.pt`. A compatible fitted weight schema is insufficient. Keep all 48 complete epoch permutations and the original public tensor digest. `cache.public_training_data` and the independent `cache_audit.public_learning_tensors` provide matching allowlisted packet/command/reward construction; original native replay remains under the authenticated parent contract.

Authenticate all six inherited actual classes and final checkpoint/configuration/tensor identities before fitting. Construct their deployment models without inherited optimizers, using the geometry-memory restoration pattern but a new six-fit manifest. Before new evaluation, authenticate all nine final models and save before snapshots; compare after snapshots and checkpoint-file hashes at completion. New history weights receive three fresh Adam optimizers and 1,152 updates each; inherited models receive none.

## 3. New trainer and saved training audit

Call `TwoObservationTrainer(initial_weights, orders, public_data, *, settings=TrainingSettings(...), expected_initial_sha256, expected_orders_sha256, expected_data_sha256, provenance, source_sha256, runtime)`. `expected_initial_sha256` is the named GRU tensor hash. **`expected_orders_sha256` is `canonical_state_hash` of the entire sealed order payload, including its integrity field; it is not merely `orders['integrity_sha256']`.** Preserve this distinction in receipts and audit checks.

Adapt `cache_study.fit_one`'s exclusive directory, update loop, flushed JSONL, exact final weights/checkpoint and retained failure pattern. Its factory and receipt fieldsets cannot be reused unchanged. The history trainer checkpoint additionally binds `provenance`, cursor-linked `rng`, `failure` and `last_attempt`; its initialization is `{weights, tensor_sha256}` rather than the old multi-family bundle. Its settings/configuration version also differs. Failed partial checkpoints are terminal and cannot authorize a retry.

The independent auditor should adapt the mathematics of `cache_audit.audit_checkpoint/audit_logs`, without calling trainer restoration or optimization. Preserve actual parameter schemas/order, exact Adam group options and IDs, complete moment shapes/finiteness, nonnegative second moments, per-parameter step 1,152, final cursor and final weight digest. Check original orders against every logged index/hash, loss-component arithmetic, public target masks, gradient-clip flags and the full log chain.

New log fields `observed_neural_sample_calls` and `rng_identity_sha256` must be checked, not discarded to satisfy the old fieldset. Reconstruct logical consumed-epoch RNG identity from saved orders and cursor. Match successful hook counters to all 12 observation updates and 11 reconstruction transitions plus ordinary prefix/rollout advances, including padding, heads and analytic cost. Check `last_attempt` and total setup/training/restoration times. These saved numerical checks do not prove every gradient or optimizer transition; proving those would require retraining, which this audit does not do.

## 4. Public-history and geometry audit wiring

For every history row and `t=0..49`, obtain packets and commands from the authenticated native record's `policy` allowlist. Pass exactly `packets[:, :t+1]`, `commands[:, :t]`, saved root/carried state, and actual `commands[:, t]` into `reacher_two_observation_audit.audit_decision(...)`. Build `expected_identity` from authenticated final weights and the independently specified actual class/configuration, not by copying `recorded_identity`.

This checks the last-two-valid anchor, sanitized unavailable angles, integer clocks, retained issued actions, exact real buffers, pending selected command and one-step carried phase. It deliberately does not numerically verify hidden vectors or decoded angles. Separately verify native execution, source/tensor identity, all candidate geometry through NumPy, four-bank saved-score CEM replay, selected re-advance alignment and complete work/timing receipts. The old scalar geometry checks are reusable; old higher-level model/version fieldsets need explicit new adapters. Preserve `executed_predictions.rewards` as the original learned-head prediction, distinct from geometry rewards used to choose actions.

## 5. Exact file boundary for 42 completed rows

For every panel, retain all nine learned models and all five references. Completed root membership must derive from this manifest, not permissive globbing, and reject symlinks, extra failed/partial files or missing rows.

- Eighteen inherited learned rows retain `episodes.npz/json`, `timings.json`, `executed_predictions.npz`, `states.npz`, `state-work.json`, and 50 `.npz/.json` pairs in each of `decisions/` and `scoring/`.
- Nine new history rows retain those payloads plus, in the current episode adapter, `started.json`, `inputs.json`, row `completed.json`, and 50 `controller-decisions/NNN.json`. Row completion hashes every row payload except itself. Its `timings.json` contains `row_payload_wall_seconds`; the final row wall includes closure and hashing and lives in row `completed.json`. Do not apply legacy `timing_row` blindly.
- Nine physics rows retain native episodes/timing, `planning.npz`, `physics-final.json` and all 50 decision/physics `.npz/.json` pairs. Particle and kinematic rows also retain `observer-final.json`.
- Six floor rows retain episodes/timing and `planning.npz`; placeholder zero scores are not predictions.

The root additionally needs the 44 inherited members and lineage sidecars, three new five-file fit directories, training/authentication phase receipts, nine before/after model snapshots, all 50 saved innovation `.npz/.json` pairs, restoration/evaluation/control boundaries, costs and exact completion membership. Keep completed execution distinct from failed scientific qualification. No prediction cohort or common-root diagnostic belongs in this experiment.

## 6. Freshness, costs and launch prerequisites

The new protocol currently offers symbolic roles only. The actual RNG layer must extend the authenticated earlier registries from the existing `prior_streams` chain, then exclude **the completed geometry-memory scored namespace and all four full-size engineering namespaces as well**. The old `geometry_memory.prior_streams()` alone returns the older geometry chain and does not add its own consumed current namespace. Preserve inherited geometry/cache/memory exclusions, particle child states and known literal NumPy/native/Torch calls. Add source attribution for newly implemented training/controller/episode/results/runner/auditor tests. Historical order replay and isolated overwritten constructors are intentional reuse, not fresh scored streams; do not retain the old receipt claim of twelve seed-zero constructors or no training work.

Retain 134,400 executed native control transitions, 78,741,504 nominal candidate transitions and 28,800 selected nominal advances as separate scopes, plus observer propagation. Charge three new fits and all reconstruction calls. Aggregate complete row walls, fit/setup/restoration, input generation/storage, source/member hashing, copies and audits without double-adding nested timers. Use the latest prerequisite's authenticated cumulative ancestry once, not cache plus its descendants again. Timing is shared-host batched throughput, not isolated latency or equal FLOPs.

Before any scored plan: finish and independently review new runner/auditor, episode/results and their tests; extend the actual source closure beyond the protocol's current minimum 104 to include every newly imported module/test; preserve the old 90 exact hashes; complete a retained whole-tree synthetic rehearsal with real file authentication, all three fresh tiny fits, six inherited engineering fits and all 42 rows; measure full-width/full64 capacity including reconstruction training and nominal physics replay; bind explicit execution/audit caps and disk reserve. Then freeze a new executable envelope separately from the current metadata-only prospective settings. `validate_preparation_bindings(...).execution_authorized` remains false and cannot serve as launch authority.

The future outer runner should retain the old proven boundary pattern: exclusive attempt before validation, mandatory final validation, all fits/restores before fresh draws, external plan/source/runtime revalidation after controls, cap checks around hashing/completion, preserved failure receipts and no silent retry. The auditor must authenticate exact saved membership both before and after verification, pass deadlines to reused scoped checks, and preserve an over-cap receipt as failed rather than success.
