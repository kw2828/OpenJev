Complete collection evidence for OpenJev's prospective query-memory comparison.

- 108 fixed collector paths: 54 TRAIN, 18 DEV and 36 TEST.
- All 17 original data payloads, original completion records, direct admission inputs and 132 frozen source records.
- Every archived member verified byte-for-byte. Checksums and a dependency map are included.
- Both upstream OTTO MIT notices are preserved.

Training is still running. This release reports no learned-model performance,
calibration, autonomous utility or architecture advantage. Packaging decoded no
numerical arrays or trajectory journals; it does not admit TEST evaluation.

The archive is collection evidence, not a self-contained native reproduction.
Original teacher weights, extracted tensors, observation kernels and the
installed runtime remain external, with their identities recorded in
`DEPENDENCIES.json`. Original receipts retain their historical paths.

Archive SHA256:
`720f65933c36fb565e4a838b52acefe2d7829e8c56f67df71b70a4ecf0839ddd`
