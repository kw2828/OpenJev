# Restore the V2 query-advantage evidence

Verify SHA256SUMS and manifest.json, then extract the tar.gz at the repository root. Every archive member has its original repository-relative name and bytes; deterministic archive headers do not change payloads. The external inventory is intentionally not an archive member.

This archive includes the complete current scientific run, independent audit, renderer, all twelve fabricated logging blocks and preserved current engineering failures, 130 unique frozen scientific/logging source files, publication sources/reports, and both MIT licenses. It does not include the original V1 partial pilot again.

This is not a standalone runtime or independently retrained model bundle. The manifest lists excluded inherited input descriptors, including original legacy weights, extracted tensors, public kernels, and qualification/input-chain records. Obtain those exact inputs and their recursive lineage from the frozen plans and prior releases, notably otto-query-gate-learning-v1 and otto-query-advantage-v1. Runtime environments are not archived.

Portable byte inspection does not imply portable strict numerical replay: the original authenticators require the recorded absolute repository/input/interpreter layout and qualified runtime, or a separately reviewed relocation adapter. None is provided. Do not rewrite historical plans or receipts to make paths pass. No scientific training or control admission follows from this archive.
