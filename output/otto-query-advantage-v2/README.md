# Paired value-of-query pilot V2 evidence

Completed and independently audited. The scientific continuation rule failed:
**7/11 conditions passed**. All 72 TRAIN paths and all 264 available label panels
were retained. No model was trained and no evaluation episodes were run.

[Results](../../research/otto-query-advantage-v2-results.md) report all conditions,
paired-label statistics, censoring and complete costs. The separate
[logging qualification](../../research/otto-query-logging-results.md) achieved
5.103x median speedup on fabricated events, with identical bytes and unchanged
per-event flush/fsync. That engineering result is not a scientific model gain.

The repository retains compact plans, engineering attempts and receipts,
supervisor records, summary reductions, independent audit outputs and figures.
The [release archive](https://github.com/kw2828/OpenJev/releases/tag/otto-query-advantage-v2)
retains every V2 payload, all twelve fabricated logging journals, all engineering
attempts, all raw native and sampler journals, all anchors and label panels, and
the complete saved audit and figure data. Its manifest lists each included
file's SHA256 and size. Extract at the repository root to restore paths.

The archive includes this allocation's sources and all 129 sources bound by the
scientific plan, plus the logging plan's nine sources. Prior model weights,
runtime and inherited input evidence remain identified by the plans and earlier
releases. This is not a standalone runtime bundle. Authenticators retain their
original absolute paths; extraction alone does not qualify a scientific rerun.

The original V1 attempt remains incomplete and unchanged. V2's metadata
aggregation failure and corrected aggregation are both preserved. Refer to the
logging report for the precise distinction between executed tests and the final
benchmark lifecycle static check. No old failure was overwritten.
