# Verified geometry study assets

Every scored result is retained whether its scientific continuation gate passes or fails. The separate engineering archive includes the failed and successful preparation attempts, repair source, and original terminal receipts; these are not effectiveness results. No upload has occurred.

For each split archive, concatenate its ordered_release_assets from receipt.json in order. These are consecutive byte segments, not standalone tar files. Verify the combined archive SHA-256 and then open the recovered gzip tar. Every archive member was reopened, streamed and checked against manifest.json. Each release asset is at most1,400,000,000 bytes, below2GB.

All80 frozen source bytes, six inherited checkpoints,51 complete controller rows,48 diagnostic roots, independent audit and final report/replay are included. Older upstream lineage dependencies remain separate releases. Packaging changes no scientific claim.
