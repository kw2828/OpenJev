# Matched teacher-cost action learning

22 September 2026. Prospective before larger continuation sampling, fitting or fresh evaluation. This tests whether continuous teacher-continuation targets improve an ordinary controller relative to analytic preference targets on the same learner-visited states. It is not a novel architecture study. Freeze exact sources, runtime, input hashes and allocation in a machine-readable plan before execution.

## Inputs and two target families

Use all 558 states in the completed [cohort preflight](otto-teacher-cohort-results.md), with its exact order, masks, public float64 beliefs and 144 episode identities. Bind its plan `dcae4eeed5e72c1352f5e32bba359c601dd2442cf7f62586774ca74499b38c94`, worker `da32d79052c0bdbd665bb409c543cfef7b2cec278b15eda080f40b1730093121`, original supervisor terminal `14409a676ab65d7751248c088dacd0bf22c03498847de1ec1a7d514e998bff9a` and independent audit `694b4f92d69df8f7dc256dfb0b142227324bda0c3514c8eae499c6c5ea6cfccf`. Verify the complete inherited source/input closure, copied belief witnesses and all support results before sampling. Preserve every anchor, including resets and tied or imprecise cost panels. No replacement, deduplication or outcome-based filtering.

The analytic arm reuses `teacher_costs` only at those exact selected rows in the authenticated original learner-TRAIN metadata. Join every selected identity, public packet and posterior witness. Require finite costs on allowed actions and `null` on blocked actions; map only blocked nulls to positive infinity. These are existing analytic heuristic preferences. Their historical generation cost is reused work, not zero-cost labeling or a matched new label budget.

The continuation arm generates mean capped action costs with the unchanged qualified sampler. Use seed **19000002**, anchor IDs **0-557**, replicate IDs **0-15**, all geometrically eligible first actions in ascending order and horizon **2188**, including the forced first action. Source and observation channels and common-uniform pairing are exactly those of `otto_teacher_rollouts.py`. Generate a new source from each public belief per replicate; never use the original trajectory's source. The fixed analytic teacher continues after the forced move. Every final packet is assimilated; discovery exactly at the horizon remains success. Unexpected errors or interruptions are missing work, not censored labels.

There are **32,304 continuations** from 8,928 replicate source draws. The maximum move count is **70,681,152**. For N=558, C=32,304, completed moves S and found continuations F, the unchanged sampler emits `67*N + 4*C + 8*S - 2*F` events, at most **565,615,818**. Its own validation adds 558 snapshots; action snapshots number C. This phase does not reconstruct the already preserved histories or repeat the preflight's support-constructor count. Save all individual costs, found/censored flags, paired reductions, source/hit draws and teacher decision events. Retain the fixed reducer's descriptive uncertainty output without using it to admit or select labels.

## Matched training

Reuse the qualified public feature map and ordinary `dense_augmented` Tanh head: **2,836→32→16→4**, **91,380 parameters**. Inputs are the exact public posterior, position, eligibility, sensing length and local forecast features. Both arms receive identical features and masks. The same eight D4 views are used during training; deployment uses one view with no ensemble or teacher fallback.

Use the qualified [common regression objective](otto-cost-regression-component.md). Analytic targets are eligible-action-centered scores divided by `0.25*max(eligible_range,1e-8)`, the centered negative logits of the historical preference distribution. Continuation targets are eligible-action-centered mean capped moves. Each arm uses its own single episode-weighted TRAIN RMS scale, floored at `1e-8`; never divide each continuation panel by its range. Save original float64 targets, scales and float32 training casts. Center predictions and targets over allowed actions, use mean squared error over allowed actions, average the eight views, then apply identical episode weights `N/(E*n_episode)` and batch means.

Fit both arms from identical fresh initial weights at each seed **10101, 10102, 10103**, in that seed order and analytic then continuation order. Save and verify paired initial exports. Use separate fresh Adam states, learning rate **0.0003**, batch size **128**, gradient norm clipping **5**, exactly **80 epochs**, CPU float32 and deterministic Torch with one numerical thread. Restart `default_rng(seed+20000)` independently for each arm so every paired epoch and minibatch receives the same row order. With 558 rows this is **400 optimizer updates per fit, 2,400 total**, and 2,142,720 augmented row presentations. Save every epoch-order hash and loss; retain the final checkpoint only. No validation-based choice, learning-rate adjustment, extra epochs or replacement fits.

Export each final checkpoint through the unchanged qualified model interface. Compare its NumPy readout with Torch on the first sixteen TRAIN features at `atol=2e-5, rtol=2e-5`, preserving all outputs. This is an export diagnostic, not an efficacy evaluation or an exact action-identity guarantee near ties. Evaluate the specified NumPy deployment for every model regardless of training losses. This common regression comparison does not replicate the older cross-entropy recipe.

## Fresh autonomous evaluation

Run all six final models and the unchanged in-bounds analytic controller on every one of **72 new cases**, yielding **504 episodes**. There are 24 cases each at sensing lengths 3, 4 and 5. Seeds are **1020001-1020024**, **1030001-1030024** and **1040001-1040024**, respectively. For zero-based case c, set initial hit `1+c%3` and block `c//3`, giving eight blocks with all three hit strata. Use regime order lambda3, lambda4, lambda5. These three seed ranges were checked against existing study code/protocols before this freeze.

The canonical arm order is `[analytic@10101, continuation@10101, analytic@10102, continuation@10102, analytic@10103, continuation@10103, analytic_inbounds]`. Rotate it left by `(regime_index*24+c)%7`. The unchanged seeded native adapter pairs sources and random uniform channels within each case. Different action paths may observe different hits. Reuse the authenticated original symmetry-study likelihood kernels and initial-hit mixture weights for all three settings, including lambda5; these are public environment parameters, not old evaluation outcomes. No new native template resets are needed. Do not decode old evaluation observations or scores.

Each new episode runs until source discovery or **2188 moves**, with no early cycling stop. Assimilate and record every final public packet, success at H and unsuccessful H separately. Require in-bounds movement, exact public/native posterior witnesses and complete reset/update lifecycles. The actor receives only the public packet and known kernel. Hidden sources, native state and draw evidence remain evaluator-only. Record every action, score vector, observation, posterior hash, outcome, controller cost and environment cost. No fitting, calibration, action override, fallback or model selection during evaluation.

## Costs and continuation rule

Report raw counts and per-hit-stratum outcomes, then mixture-weighted success, capped moves and controller seconds using the authenticated positive-hit weights for each setting. Compute equal-seed family means from all three fits. Paired block gains use those same weights and equal-seed means; they are descriptive robustness checks, not independent trajectory samples or a significance test.

Controller time includes actor/feature initialization, feature construction, head or analytic scoring, action selection, public posterior updates, checkpoint loading and shared deployment-module setup. Allocate each checkpoint's actual restore time across its 72 episodes and common model-module setup across the 432 learned episodes. Count actor/filter work for analytic control too. Record logger and resource-monitor costs separately, never subtract computational work as I/O. Native simulation is reported separately. Also report complete collection, fitting, authentication, setup, serialization and total process costs. The newly paid continuation label cost is charged to that method; the analytic arm reuses historical labels. Equal optimizer updates do not imply equal total computation.

Advance this target recipe only if **all 33 checks** pass:

- In each of the three settings, analytic control must achieve weighted success at least 95% (three positive-control checks).
- Every continuation fit in every setting must achieve weighted success at least 95% and capped moves no greater than 105% of analytic control (18 competence checks).
- In every setting, the continuation family's success must be at least the analytic-target family's success, its capped moves no greater than 95% of that family's capped moves, its controller cost no greater than 105%, and its paired move gain positive in at least six of eight blocks (12 relative checks).

Keep every scalar condition, all six fits and failed comparisons visible. Passing would justify fresh confirmation and a later matched architecture test; it would not itself establish architectural novelty, Bayesian benefit or robotics transfer. Failure does not isolate the cause among labels, precision, coverage, capacity and optimization. A second environment and a specific tested mechanism remain necessary for the broader research objective.

## Single bounded execution and evidence

Run once under the existing original suspend-inclusive supervisor: **7200 seconds, one numerical thread, 4 GiB peak RSS and 16 GiB output**, covering authentication through labeling, fitting, evaluation and final payload closure. Keep all original source files unchanged. Bind reviewed new sources, fabricated component checks and exact runtime before generation. Use an exclusive run directory, preserve attempts and pending work on failure, and require the original successful parent terminal before claiming completion. Training-only completion cannot be called a completed study.

Sampler events use gzip level 1, with each panel closed and flushed to durable storage before its completed boundary is published in a separate durable journal. An interrupted panel is incomplete even if some records can be recovered; no continuation or retry is allowed. Preserve the original sampler event schema, with at most 512 uncompressed bytes per event. The theoretical raw event bound is **289,595,298,816 bytes**, which exceeds the compressed output allocation. Compression is not assumed to guarantee a fit. Enforce the 16 GiB limit on actual files throughout the run and fail without extending the budget if it is reached. The small pilot's speed does not bound late-state collection cost.

Save feature/target arrays, scales, all six initial/final checkpoints, optimizer update counts, training curves, full evaluation evidence and every criterion. A separate saved-evidence audit may authenticate/replay recorded computations under its own declared scope; it cannot add labels, optimizer updates or new evaluation cases. Publishing an unaudited provisional result must say so. No success, compute or novelty claim may be based only on fitting loss or a passing implementation check.
