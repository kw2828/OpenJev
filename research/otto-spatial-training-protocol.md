# Fixed-data spatial value learning

This protocol tests the five already [qualified model families](otto-spatial-qualification-results.md) on the original authenticated scalar-return dataset. It changes representation and capacity, with identical examples, targets and schedule. It does not test connectome wiring, recurrence, unknown dynamics or a new learning algorithm. The historical failed studies and their criteria remain unchanged.

## Models, data and schedule

Use every family in this order for each seed: `spatial`, `neighbor_free`, `cnn`, `dense128`, `statistics`. Seeds are 10101, 10102 and 10103, in that order. Parameter counts are respectively 737, 737, 801, 1,411,841 and 225. Architecture, feature order, raw-mass conventions, zero-padding, signed outputs and bias behavior are exactly those of the immutable qualified `otto_spatial_value.py`. No clipping, symmetry ensemble, architecture change or candidate-specific repair is permitted.

Use all 5,589 original TRAIN prefixes from 192 complete teacher episodes and all 1,109 exposed VALID prefixes from 48 episodes. Authenticate original cache bytes, source/runtime lineage, worker completion and independent audit before decoding arrays. Verify exact row order, public position, regime/sensing identity, posterior bytes and mass, legacy feature construction and `(total_steps-prefix_index)/64` targets. No evaluation episodes, additional demonstrations or recollection are inputs.

Construct centered `105x105` arrays by placing each original float64 `53x53` belief at `[52-qx:105-qx,52-qy:105-qy]`. Position and sensing length remain explicit even for zero mass. Training receives one float32 cast of this public representation. Final deployment diagnostics receive the original float64 representation. Both views and their costs must be disclosed. Expanded per-cell spatial features are constructed within each batch, not retained for all TRAIN rows.

Each of fifteen fits starts fresh with the qualified local-generator He hidden initialization, zero final weights and bias, and fixed `c0=float32(mean(TRAIN targets,dtype=float64))`. Equality of the initial value function follows from those exact final tensors and the common baseline within each arithmetic view; checkpoint inspection verifies it without extra initial forward calls. Spatial and neighbor-free heads also share their seed-paired initial hidden tensors. Disposable qualification weights and historical fitted checkpoints are forbidden initializers.

Use fresh Adam at learning rate 0.001, gradient norm clipping at 5, uniform row MSE, 80 epochs, batch128, and NumPy local permutations from `seed+20000`. Reset that generator per fit, pairing sample order across families at a given seed. Each epoch contains 43 full batches and one 85-row tail, so each fit makes 3,520 updates and the complete study makes **52,800 updates**. Record every order, batch identity, pre-update loss, gradient norm and optimizer step count. Epoch curves aggregate those existing batch losses; they are not fresh model evaluations or final-epoch full-dataset losses.

Save every initial and fixed final checkpoint. No early stopping, best-checkpoint selection, validation tuning, replacement seed, extra epoch, failed-arm repair or partial family promotion. Report all fifteen final models regardless of scalar ranking. No scalar gate admits architecture efficacy or selects which family gets autonomous evaluation.

## Exact recorded work and final diagnostics

The worker records attempted/returned calls around each actual model forward, backward and optimizer update. Across fifteen fits there are 52,800 calls of each, plus 15 initializations, 15 optimizer constructions, 30 checkpoint exports, 15 NumPy deployment constructions and 15 Torch64 restores. These 90 setup/export operations and 158,400 training operations are charged and logged.

Final predictions use batches of sixteen, with five-row tails. For each of fifteen final checkpoints, evaluate every TRAIN and VALID row with independent NumPy float64 deployment and a Torch64 copy of the same saved float32 weights. There are 350 TRAIN and 70 VALID calls per backend per fit: **6,300 NumPy and 6,300 Torch64 forwards**, covering 100,470 rows per backend. Save both complete prediction arrays. Require each normalized pair to satisfy `abs(numpy-torch) <= 1e-8 + 1e-10*abs(torch)` with finite values. This checks scalar outputs on these original public states, not every future sixteen-branch action tie.

The full allocation is **171,090 recorded operations**, including **65,400 model forwards**, 52,800 backwards and 52,800 optimizer updates. The fixed payload closure contains 56 files before the worker receipt. Native resets/steps and external model calls are zero. Shared-source authentication and file I/O are not model forwards, but remain included in complete elapsed work.

Report per-fit and equal-seed family normalized MSE, physical MAE, negative prediction counts and value ranges for both splits. Primary scalar summaries use original-float64 NumPy predictions. Preserve Torch64 comparisons separately. The earlier legacy-feature duplicate-input floor is not reused: this explicit-input representation and arithmetic view differ. No confidence interval or generalization claim follows from the exposed VALID set or the shared episode prefixes.

## Bounds, execution and independent audit

The externally pinned JSON plan binds the full immutable historical source/data/runtime lineage, qualified spatial implementation and evidence, this protocol, new runner/freezer, independent tests and prospective auditor. Use one CPU thread with the existing Torch/NumPy environment. No installation, hardware switch, paid API or background model service is part of the run.

The original suspend-aware supervisor enforces **7,200 seconds** for the worker. Peak RSS is limited to **8 GiB** and the exclusive output directory to **2 GiB**, checked during operations. These RSS/output checks are sampled, not a hard address-space reservation. Preserve failed, interrupted and partial work with journals and failure receipts; do not restart under this plan. A completion receipt alone is insufficient without the original supervisor's successful exit, no timeout and absent process group. Authenticate sources and inputs again at completion.

The synthetic projection of 1,334 seconds (2,668 with a planning allowance) excluded some scientific overhead. The full cap includes data authentication/preparation, geometry and casting, fitting, all final diagnostic passes, checkpoint writing and final checks. Record preparation, fit, prediction and deployment setup costs separately without subtracting them from total worker time. Per-operation and fit intervals use the inherited performance timer; the complete worker and parent use the suspend-inclusive clock. Parent time encloses worker time and must not be added to it as independent work.

A separately bounded saved-model audit has **1,800 seconds, 4 GiB RSS and 128 MiB output**. It authenticates source/data/process joins, checks the full work journal, independently regenerates paired permutations and batch membership, validates loss aggregation, initial tensor/function constraints, checkpoint schemas and recorded optimizer allocation. It replays all 100,470 final public rows using the qualified NumPy inference implementation, for exactly **6,300 saved-checkpoint forwards**, and checks both saved backends and every reported metric. This is real model computation with separately recorded cost. It does not rerun optimization, native environments or teacher collection.

The replay shares the qualified NumPy network algebra with the producer; independent geometry/data/metric reconstruction and the producer's saved Torch64 outputs provide additional checks. The existing engineering suite supplies direct neighborhood-sum, 1x1-equivalence and CNN boundary oracles. Actual historical optimizer execution and durations remain authenticated producer evidence, not independently regenerated trajectories of training.

## Advancement remains an autonomous-control question

Successful completion admits preparation of a separate autonomous protocol, not a performance claim. Preserve all fifteen fixed final heads and the unchanged analytic controller in that comparison, irrespective of scalar results. Use fresh paired cases for supplied sensing lengths 3, 4 and 5. Sensing length 5 is an unseen supplied parameter on the same board, not learned unknown dynamics. Exact cohorts, order, call/time limits and complete utility/cost predicates must be frozen before any new autonomous outcome is observed.

The absolute competence anchor remains at least 95% weighted success and capped moves at most 1.05 times analytic control for every candidate seed and setting. A spatial advantage additionally needs meaningful paired gains over ordinary controls and a demonstrated utility-versus-compute tradeoff, with all branch construction, prediction, setup and public updates included. Failed episodes retain their full horizon. A better regression loss alone cannot satisfy these requirements, and pooling away a failed seed or scenario is not permitted. Further environment validation and a distinct mechanism would still be needed for an ICLR contribution.
