# Spatial training integration after synthetic qualification

**Unfrozen implementation handoff, not an executed study.** The [engineering qualification](otto-spatial-qualification-results.md) makes the proposed five-family training workload feasible under its planning estimate. It does not authorize a scientific result or establish control efficacy.

The original return-value preparation already saves `beliefs: float64[N,53,53]`, `positions: int64[N,2]`, `sensing_length: float64[N]`, `target: float32[N]` and legacy features. The inherited cache contains 5,589 TRAIN prefixes from 192 episodes and 1,109 exposed VALID prefixes from 48 episodes. Row metadata supplies `public.position`, regime and pre-action prefix index. There is no need to reconstruct position or sensing length by dividing the old mass-scaled feature tail.

The adapter should reproduce the original centering operation exactly: allocate zero `105x105`, then insert each cached belief at `[52-qx:105-qx, 52-qy:105-qy]`. Retain the float64 public representation for deployment diagnostics; cast this same centered array to float32 for training. Pass position and sensing explicitly. Verify metadata, posterior hashes, legal coordinates, original feature consistency and geometry round-trip before fitting. Construct expanded spatial channels within each batch, not a retained expanded TRAIN cache.

Reuse source/runtime/receipt/terminal authentication from the existing capacity/conditioning lineage, including exact descriptors for the original return-value cache. Do not reuse its flattened-input fit or prediction callbacks: the new heads accept `(centered, positions, sensing)` and permit at most 128 rows, whereas older scalar diagnostics used batches of 256. The original source reference is [study_otto_return_value.py](../scripts/study_otto_return_value.py); the authentication example is [study_otto_capacity.py](../scripts/study_otto_capacity.py). Both remain frozen and must not be modified.

For the proposed fifteen fits, retain identical uniform `(T-t)/64` targets and `c0=float32(mean(TRAIN target, dtype=float64))`, fresh Adam at 0.001, gradient clipping at 5, 80 epochs, batch128 and three paired seeds. Each seed's data orders use the same local `seed+20000` generator across all five families. This is 52,800 optimizer updates. Every head uses the new common zero-final-layer initialization; disposable qualification weights and historical checkpoints must not initialize a scientific model. Final checkpoints are fixed by schedule. VALID is descriptive, not a model, hyperparameter or epoch selector.

Independently audit initialization, every sample order/update allocation, the exact model parameter shapes, all saved final predictions and original float64 deployment parity. Reconstruct full explicit public tuples if reporting duplicate-input lower bounds. The older floor computed from legacy float32 feature bytes is not automatically a lower bound for the newly explicit position/sensing or float64 deployment input view; omit it unless independently derived for a stated view. Report both training and deployment arithmetic, rather than calling them identical computations.

The runtime projection includes only final float32 diagnostics. Any per-epoch diagnostics or saved float64 scoring and audit must receive explicit call/time allocations in the future plan. At batch16, complete TRAIN/VALID float64 scoring alone requires 420 calls per head (including two five-row tails), or 6,300 across fifteen fits. Charge dataset authentication, centering/casting, shuffling, feature work, fitting, saving/loading and final diagnostics. Neither equal update counts nor nominal MAC counts imply matched elapsed cost.

No scalar comparison can establish the requested architecture advantage. Autonomous evaluation must retain all ordinary controls and the strong analytic reference on fresh cases, with absolute competence, paired utility and full-cost rules frozen before running. The exact scientific source closure, schedule, scalar reporting, deployment qualification and autonomous cohort still need separate admission. The goal remains open.
