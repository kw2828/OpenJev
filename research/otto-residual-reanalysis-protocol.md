# Prior-DEV Bayesian residual reanalysis

This is a separate development-only analysis of the 18 paths collected by the
[closed screen](otto-residual-runtime-repair.md). Its metadata-only evaluation
planner failed before any numerical evaluation. That attempt stays closed.
This analysis is registered after collection and before numerical evaluation;
it is not a fresh collection or held-out confirmation.

## Fixed scientific question

Retain the [original design](otto-residual-estimator-design.md), all nine methods,
all three training seeds and all nine original checkpoint files. Full-covariance
RLS remains the sole candidate. The prior/noise grid remains 0.01, 0.1, 1 and 10;
select by the worst setting's mean later gap with the original ascending exact
tie rule. All 72 views, the 13 usefulness conditions and separate covariance
and correction-strength contrasts remain unchanged. No training, replacement
data, adaptive observations, altered denominator or control promotion is allowed.

The only allowed census is the original 18-path DEV collection, receipt SHA256
`245c635dac36cad4b4e3d0b074e623c2bb7e01f12adff5031e93a56d5a73baba`.
The original technical closure SHA256 is
`e5c1e13eea4c25a24a311cde34fa3cece49b80f170556079c0f638ac64c4bcb5`.
The old screen's reserved confirmation paths and the earlier study's TEST
remain unused and unadmitted. Any later confirmation requires a distinct fresh
cohort and separate prospective registration.

## Environment handoff

A standard-library metadata bridge runs under the exact native collection
interpreter and authenticates the original sources, runtime and successful
collector closure. Its own original 60-second supervisor must close successfully.
The evaluator consumes this receipt without invoking native-runtime checks in
the training interpreter. Both original runtime requirements remain intact.
Numerical libraries are denied in bridge and evaluator planning processes.

Before numerical execution, require fabricated tests, lint, the unchanged
worst-horizon capacity rule and a real-process metadata handoff between both
interpreters. Record negative cases for changed input hashes, a wrong interpreter
and incomplete bridge evidence. Publish the evaluation plan with source, input,
checkpoint, bridge and protocol hashes before starting its worker.

## Execution and stopping

Use the existing numerical cache/replay/metrics/audit implementation unchanged.
Extraction uses batch one, chunks of 32 and period-four teacher observations.
All 72 outputs must be retained. Evaluation and independent saved-output audit
each have an original 900-second supervisor, 4 GiB RSS limit and respectively
1 GiB / 256 MiB output limits. The source-tested numerical operation counts and
independent decision reconstruction are unchanged. Bridge and planning work
are reported separately as real overhead.

The independent audit makes no neural-model, optimizer, teacher or simulator
calls. Its result becomes official only after the original auditor closes and
the metadata closure validates all upstream evidence. Any technical failure or
failed usefulness condition closes this reanalysis. No retry, cap extension,
replacement seed or post-result change can rescue it.

A development pass permits considering a separately registered fresh-cohort
confirmation. It establishes no autonomous utility, calibrated probabilities,
connectome advantage, new learning rule or paper-level novelty. Keep every
method, fit seed and ratio visible regardless of the outcome.
