# Saved branch/value diagnostic

This posthoc explanation follows the complete twelve-fit [typed-decision study](dialogue-typed-results.md), whose continuation rule failed, with 5 of 9 checks passed. No result can change that decision. There is no new fit, model call, probability repair, controller, threshold or confirmation set.

Use all twelve published final distributions and the existing canonical evaluation rows. Bind run completion 65bd57084dbc7f7b99b782e140bae2e18f25b9e237e89a0132cead5fbfe2a095 and published summary 3969bcb7803d3f625a90a9e165a775bb9108f8301b9f225d198eed2e9e84a81c. Authenticate the complete run through the unchanged corrected reporter, then operate on saved float32 log probabilities promoted to float64. Do not load weights or access new data.

For each supported candidate, public schema types define three branches: NOT_MENTIONED, DONTCARE and concrete values. Compute each branch's log mass with stable logsumexp. Target NLL equals target-branch NLL plus target-value conditional NLL within that branch. Keep raw logs and their permitted floating-point mass deviation. Do not floor, clip or renormalize. Singleton branch conditional NLL is zero; a tiny negative branch NLL caused by permitted roundoff is preserved and reported. Require loss additivity within 1e-10.

Keep the actual first-argmax candidate decision unchanged. Its errors partition into correct, wrong selected branch, or wrong value within the correct selected branch. The latter can only occur for a concrete target. A separate branch-mass argmax can disagree with the actual selected candidate's branch: report it descriptively, never substitute it. Conditional value correctness supplies the true branch and is a label-privileged diagnostic, not a deployable controller. Record candidate ties and the 3-by-3 target/selected branch confusion matrix.

Report all/held-out-service/other-service panels, each over all/changed/retained rows. Also report every held-out service's changed rows and every target type's primary changed rows. Preserve row, equal-service and equal-dialogue means, row/dialogue/service/query support, and undefined empty means. The primary remains the 578 changed rows of held-out services. Verify original NLL and actual accuracy against the published report for all nine core groups at every fit.

Describe four paired contrasts at every seed: typed-balanced minus flat-balanced, typed-stratum minus flat-stratum, flat-balanced minus flat-stratum, and typed-balanced minus typed-stratum. Record correct/correct, correct/wrong, wrong/correct and wrong/wrong row counts and the three-way error transition matrix. Net accuracy difference must equal (wrong-to-correct minus correct-to-wrong)/N. Conditional repair and harm rates have different denominators and are not subtracted as an accuracy change.

Decompose each primary loss difference by target type and by paired correctness category. Each contribution uses the full primary row or equal-service denominator, so the contributions exactly reconstruct the primary difference. Empty contributions are zero with zero support, while empty conditional means remain undefined. Do not choose a favorable seed or redefine the primary comparison.

Run synthetic edge cases, review and publish this source before a single saved-output diagnostic in an exclusive output directory. Preserve any failure. Independently recompute the primary algebra and paired counts with a separate reader. Outputs are descriptive error analysis, not causal identification of what training learned, calibration, new architecture novelty or fresh generalization. Every row was historically exposed training data and every model received the correct previous value.
