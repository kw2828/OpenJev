{
  "id": "bayesian-doom-v2",
  "status": "frozen-development-protocol-not-confirmation",
  "question": "Does posterior uncertainty improve a learned firing controller beyond the same MAP predictor and simple confidence thresholds?",
  "scope": "Structured engine observations; rule-based steering; binary hit outcome for an executed firing window. Not Jev RLCD reproduction or language-model RL.",
  "training_seeds": [
    101,
    202,
    303,
    404,
    505
  ],
  "training_episodes_per_seed": 16,
  "training_episode_seed_rule": "10000 + replicate_index * 100 + episode_index",
  "train_scenario": "defend_the_center",
  "evaluation_scenarios": [
    "defend_the_center",
    "defend_the_line"
  ],
  "evaluation_episode_seeds": [
    40000,
    40001,
    40002,
    40003,
    40004,
    40005,
    40006,
    40007,
    40008,
    40009
  ],
  "tics_per_step": 7,
  "max_steps_per_episode": 180,
  "training_fire_probability": 0.6,
  "training_random_steer_probability": 0.2,
  "feature_definition": "intercept, visible, absolute aim error clipped to 1, half width, distance/1000 clipped to 2, health/100, ammo/30 clipped to 2",
  "prior_precision": 1.0,
  "newton_iterations_max": 40,
  "gauss_hermite_nodes": 20,
  "action_cost": 0.25,
  "lower_credible_z": 1.645,
  "policies": [
    "rules",
    "tiny_imitation",
    "map_hit",
    "bayes_mean_hit",
    "bayes_lower_hit"
  ],
  "primary_metric": "successful commanded firing windows - 0.25 * commanded firing windows",
  "secondary_metrics": [
    "kills",
    "health",
    "commanded firing windows",
    "raw hit count",
    "survival",
    "Brier and log loss on common audit and selected commanded-fire windows",
    "decision latency"
  ],
  "feedback": "Condition on actually issued fire buttons with ammo > 0 and non-pacifist directive. Label HITCOUNT increase > 0 during that seven-tic window. Wait windows have no fitted label; cooldown failures count as zero. This forecasts command-window success, not per-shot accuracy.",
  "evaluation": "Frozen weights; no test-time fitting. Same episode seeds for all policies. Rules and tiny baselines are evaluated once and shared across model replicates. Aggregate within each training replicate, then paired differences across five replicates; shared test seeds limit independence.",
  "stop_rule": "One run; at most 600 episodes and 30 wall-clock minutes. No paid API. Do not tune based on evaluation results. Mechanical corrections require a separate documented run.",
  "claim_boundary": "New development pilot after measurement failure in v1, not confirmation. Selected-action calibration does not establish counterfactual validity or safety. Seven-tic command-window success includes weapon cooldown and delayed effects. Tiny action preference is not scored as a hit probability. No proprietary RLCD reproduction or novel RL algorithm claim.",
  "common_audit": "A fixed randomized behavior episode on each fresh evaluation seed supplies identical commanded-fire outcomes to every model. Abort if a scenario has no audit outcomes.",
  "parent": "bayesian-doom-v1; event-definition amendment after ammo-replenishment discovery"
}
