# Fixed 16-versus-64 teacher-target precision comparison

22 September 2026. Prospective before additional continuation sampling, fresh fitting or evaluation. The [previous matched study](otto-teacher-learning-results.md) regressed in all three settings and failed 28 of 33 conditions. This new experiment tests one explanation: finite-sample error in its action-cost targets. It does not promote the failed recipe or test a new architecture. Freeze the source, input, runtime, engineering and allocation manifest before execution.

## Immutable inputs and additional labels

Use the identical 558 public TRAIN anchors from 144 historical learner episodes, in the same order, with the same eligible actions and captured float64 posteriors. Retain reset states, duplicate states, late prefixes, ties and uncertain panels. Authenticate the completed predecessor's empirical plan `7c155b10b630ee6fb7b16e302accfc0883b4a39ff7a0cb6f94fe4b6ffb0bec90`, worker `f55f19acc8b8b22ca936b78a1023835556b1ecb80b9bb84bbaa1b3dc661a0bf3`, original terminal `4dd9a826c8bfd0747e429ac0f3471b5e1614b03fe78f16f87e6a7834e19ebdf7`, agreeing V2 audit `3ef858eb393a2896a48e9ec7bd9a9feb4600a3c4c3111b8ec3484829a56826d0` and audit terminal `b0278cdd9d97f20683c9026da8ba5083db71d1661b81f768a2cf8fc3254c0270`. Bind its complete source/input/payload closure and the preserved first audit failure; do not rewrite any inherited file.

Reuse all original replicate IDs **0-15** as recorded. Generate exactly **16-63** with the unchanged qualified sampler, namespace, source/hit channels, seed **19000002**, anchor IDs **0-557**, all eligible first actions in ascending order, and horizon **2188** including the forced move. Each new replicate samples its own source from the public posterior, shared across its first actions; observation uniforms are paired by replicate and local step. No original hidden trajectory source, unsupported-state repair, replacement, confidence filter or adaptive allocation is allowed. Assimilate every final packet. A found source exactly at the horizon is success; unsuccessful full-horizon paths are censored costs, and interrupted work is failure.

The additional allocation is **26,784 source draws, 96,912 continuations and at most 212,043,456 moves**. For completed new moves S and found continuations F, the unchanged sampler emits `195*558 + 4*96912 + 8*S - 2*F` events. The maximum is **1,696,844,106 events**, or **868,784,182,272 uncompressed bytes** at the 512-byte event limit. Each new panel contains only its 48 new replicates and closes durably before completion is recorded. Keep every new operation and draw event. Never regenerate the old sixteen replicates.

Join old and new records by anchor, replicate and first-action ID. Require exactly 64 records per eligible action, with integer capped step totals, and form their mean once by dividing that integer sum by 64. Retain the original sixteen-sample means as the reference. Save raw means, original records or authenticated references to them, added records, combined totals and all found/censored flags.

Report four disjoint sixteen-replicate block means for IDs 0-15, 16-31, 32-47 and 48-63, along with descriptive paired cost variance and action disagreements. R16 is a subset of R64, so their agreement is not independent validation. These diagnostics never alter anchors, targets, weights, computation, checkpoints or the continuation rule. A saved-label noise diagnostic may motivate this study; it is exploratory, not efficacy evidence.

## Matched learning with a shared fixed scale

Both arms use continuation-cost centering over allowed actions. **Do not call the old two-family target constructor**, which would apply analytic range normalization to the first arm. Do not normalize any continuation panel by its range.

Reuse the predecessor's features, masks, episode weights, R16 centered float64 targets, R16 float32 training targets and global R16 RMS scale **byte-for-byte**. The saved scale is approximately **1.9131089760854865**, but bind its exact recorded value rather than a rounded literal. Construct R64 eligible-centered float64 means using the qualified NumPy reduction order, then divide by that same original R16 scale and cast to float32. R64's newly estimated RMS is a diagnostic only. It must not change training. Preserve exact original and cast arrays. Each episode receives equal total weight, `558/(144*n_episode)`.

Use the same ordinary `dense_augmented` Tanh head, **2836→32→16→4**, **91,380 parameters**, qualified feature map and eight D4 training views. The loss is the existing eligible-action-centered mean squared error, averaged over allowed actions and eight views, then multiplied by the common row weights and averaged over each batch. The training function adds no confidence weights, auxiliary loss, recurrence, teacher fallback or ensemble.

Use fresh paired initialization seeds **20101, 20102, 20103**, in that order, R16 then R64. Save both initial exports and verify exact paired weights before either model updates. Use separate fresh Adam states, learning rate **0.0003**, batch **128**, gradient clipping **5**, and exactly **80 epochs**, with `default_rng(seed+20000)` restarted per arm for identical permutations. This is **400 updates per fit, 2,400 total**, and **2,142,720 augmented row presentations**. Train on CPU float32 with deterministic Torch and one numerical thread. No validation, checkpoint selection, learning-rate change, extra epochs or replacement fits. Save all six final models plus initial checkpoints. File exports must reload byte-exactly; retain the existing first-sixteen-TRAIN NumPy/Torch diagnostic at `atol=rtol=2e-5`, which does not guarantee identical choices near ties.

## Fresh full-horizon control

Evaluate every final model and the same in-bounds analytic controller on **72 fresh cases**, giving **504 episodes**. For sensing lengths 3, 4 and 5 use seeds **1050001-1050024**, **1060001-1060024** and **1070001-1070024**. For zero-based case c use initial hit `1+c%3` and block `c//3`. Require a pre-freeze source/protocol/ledger check of these reservations. The known public likelihood kernels and positive-hit mixture weights are inherited unchanged. Length 5 remains absent from training but its kernel is supplied at evaluation.

Canonical arm order is `[r16@20101, r64@20101, r16@20102, r64@20102, r16@20103, r64@20103, analytic_inbounds]`, rotated left by `(regime_index*24+c)%7`. Use the unchanged seeded native adapter, paired sources and overlapping source/hit uniform channels. Keep hidden state and random witnesses evaluator-only; the actor receives only the public state and supplied kernel. Record the exact full public/native posterior agreement, every action, score, observation, draw witness and final update. All arms run until discovery or **2188 moves**, including unsuccessful cycling cases. No fallback, action override, recalibration or fitting occurs during evaluation.

## Costs and the unchanged 33-condition rule

Report every fit and setting, raw found counts, initial-hit strata and eight paired blocks. Compute the same positive-hit-mixture-weighted success, capped moves and complete controller seconds; family values average all three fits. The 504 episodes share 72 environmental cases and are not 504 independent samples. Block wins are descriptive checks, not significance tests.

All **33 conditions** must pass to advance this precision recipe:

- Each setting's analytic positive control has weighted success at least 95%: three checks.
- Every R64 fit in every setting has weighted success at least 95% and capped moves at most 105% of analytic control: eighteen checks.
- In each setting, the R64 family has success at least R16, capped moves at most 95% of R16, positive paired move gain in at least six of eight blocks, and controller cost at most 105% of R16: twelve checks.

Compute complete controller intervals exactly as in the predecessor: actor/feature initialization, feature construction, scoring, selection, public filtering, each checkpoint restore allocated across its 72 episodes and common model-module setup across 432 learned episodes. Retain the first inference. Record native environment work separately. Only measured journal I/O is excluded from controller intervals; no feature/filter work is classified as logging. Report all disjoint collection, preparation, training, setup and evaluation stages, final closure and original process wall time. This remains a single-host CPU timing pass.

Report historical R16 label acquisition separately from newly paid R48 acquisition. Standalone R16 labeling uses the inherited sixteen-sample cost; standalone R64 labeling includes that inherited cost plus the new forty-eight-sample phase. Allocation across three fits may be shown but must not double-count shared historical work in the physical new-study total. Equal optimizer work does not imply equal total compute. Smaller target variance or training loss alone cannot pass this study.

## Single bounded execution

Run once under the qualified suspend-inclusive supervisor: **10,800 seconds, one numerical thread, 4 GiB peak RSS and 32 GiB output**, covering input authentication, new labels, all six fits, 504 episodes and payload closure. Use exclusive output paths, durable panel/episode boundaries, recorded attempts and pending operations, and the original successful supervisor terminal. There is no retry, resume, omitted state, replacement seed, budget extension or early stop based on results. Preserve a partial failed run without promoting it.

The predecessor took 342.20 seconds for its sixteen-sample collection and 1210.95 seconds for evaluation. Linear extrapolation gives about **1026.60 seconds** for the added sampling and roughly **2250 seconds** for the whole new study. These are planning estimates, not guarantees or worst-case bounds. The theoretical raw journal bound exceeds the actual compressed-output allowance. Check actual file sizes throughout and fail if a resource limit is reached; compression is not assumed to guarantee completion.

Require reviewed source changes and meaningful fabricated checks for integer merging, the common scale, immutable R16 preservation and paired training before freezing the machine plan. A separately bounded saved-record audit can verify the complete new result without generating additional scientific work. Keep all criteria and failed comparisons visible. Better control would support this precision intervention; it would not establish novelty, connectome learning, recovered temporal memory or robotics transfer. The broader research goal still requires competent comparisons, a specific mechanism and confirmation in another environment.
